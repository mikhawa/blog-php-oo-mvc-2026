<!-- Navigation moderne pour le front -->
<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
    <div class="container">
        <a class="navbar-brand fw-bold" href="{{ racineURL }}">
            <i class="bi bi-newspaper me-2"></i>Mon Blog
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ racineURL }}">
                        <i class="bi bi-house-door me-1"></i>Accueil
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link disabled text-white" href="#">
                        <i class="bi bi-tags me-1"></i>Catégories :
                    </a>
                </li>
                {# A la place du foreach, tant qu'on a des catégories #}
                {% for item in categories %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ racineURL }}category/{{ item.getCategorySlug() }}">
                            {{ item.getCategoryTitle() }}
                        </a>
                    </li>
                    {# si pas de catégories #}
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" href="#">
                                Pas encore de catégorie
                            </a>
                        </li>
                {% endfor %}
                {# Si on est connecté #}
                {% if session.user_id %}
                    {# si le role_name est administrateur #}
                    {% if session.role_name == 'Admin' %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ racineURL }}admin/">
                               <i class="bi bi-speedometer2 me-1"></i>Administration
                            </a>
                        </li>
                    {% endif %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ racineURL }}user/{{ session.user_login }}">
                            <i class="bi bi-person me-1"></i>{% if session.user_real_name %}{{ session.user_real_name }}{% else %}{{ session.user_login }}{% endif %}
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ racineURL }}disconnection/">
                            <i class="bi bi-gear me-1"></i>Déconnexion</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ racineURL }}connection/">
                            <i class="bi bi-gear me-1"></i>Connexion
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
